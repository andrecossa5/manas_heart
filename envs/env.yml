name: manas_heart
channels:
  - bioconda
  - conda-forge
dependencies:
  - samtools=1.21
  - bcftools=1.21
  - gatk4=4.6.0.0
prefix: /path/to/conda/envs/manas_heart  # change to absolute path on each machine
